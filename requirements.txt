# --- Core (Phase 0) ---
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9      # form/file uploads
httpx>=0.27                  # outbound API calls (weather, Garmin, Strava, ORS)

# --- Data parsing (Phase 1+) ---
fitparse>=1.2                # .FIT activity files
gpxpy>=1.6                   # .GPX route/activity files

# --- Integrations (Phase 2/3) ---
icalendar>=5.0               # .ics calendar parsing
caldav>=1.3                  # CalDAV (iCloud/Google) calendar sync
pywebpush>=2.0               # PWA web push notifications

# --- Dev (see requirements-dev.txt) ---
# pytest, ruff, etc.
